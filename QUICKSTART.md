# 🚀 دليل التشغيل السريع

## خطوات التشغيل

### 1. تشغيل Migrations والـ Seeders
```bash
php artisan migrate:fresh --seed
```

هذا الأمر سيقوم بـ:
- ✅ إنشاء جداول قاعدة البيانات
- ✅ إضافة مستخدمين تجريبيين (Admin + Customer)
- ✅ إضافة منتجات وأصناف وشركات تجريبية

### 2. تشغيل التطبيق
افتح نافذتين في Terminal:

**النافذة الأولى - Laravel Server:**
```bash
php artisan serve
```

**النافذة الثانية - Vite (Tailwind CSS):**
```bash
npm run dev
```

### 3. الوصول للتطبيق

#### 🔐 تسجيل الدخول كمدير (Admin)
- الرابط: http://localhost:8000/login
- البريد: `admin@admin.com`
- كلمة المرور: `password`

بعد تسجيل الدخول ستنتقل إلى: http://localhost:8000/admin

#### 👤 تسجيل الدخول كزبون (Customer)
- الرابط: http://localhost:8000/login
- البريد: `customer@test.com`
- كلمة المرور: `password`

بعد تسجيل الدخول ستنتقل إلى: http://localhost:8000/client/catalog

---

## 🎯 المميزات المتاحة

### لوحة الإدارة (Admin Panel)
✅ إدارة المنتجات (إضافة، تعديل، حذف)
✅ إدارة الأصناف
✅ إدارة الشركات
✅ إدارة المستخدمين (إنشاء حسابات جديدة)
✅ إدارة الزبائن
✅ استلام الطلبات والموافقة/الرفض
✅ فواتير الشراء والمبيعات
✅ لوحة تحكم بالإحصائيات

### واجهة المستخدم (Client)
✅ عرض المنتجات مع تصميم جذاب
✅ البحث عن المنتجات
✅ الفلترة حسب الصنف والشركة
✅ الترتيب (الأحدث، السعر، الاسم)
✅ إضافة المنتجات للسلة
✅ عداد السلة الديناميكي
✅ إدارة السلة (تعديل الكمية، حذف منتجات)
✅ إرسال الطلب للموافقة

---

## 🎨 التصميم

التطبيق يستخدم:
- **Tailwind CSS** للتصميم
- **Gradients** جميلة ومتدرجة
- **Animations** سلسة وناعمة
- **Hover Effects** تفاعلية
- **Responsive Design** يعمل على جميع الأجهزة

---

## 📝 ملاحظات مهمة

1. **السلة تعمل فقط للزبائن (Customers)**
   - المدير لا يمكنه إضافة منتجات للسلة
   
2. **الطلبات تحتاج موافقة الإدارة**
   - عند إرسال الطلب، يذهب للإدارة للموافقة
   - الإدارة يمكنها الموافقة (يتم إنشاء فاتورة مبيعات) أو الرفض (يتم حذف الطلب)

3. **التحقق من المخزون**
   - لا يمكن إضافة كمية أكبر من المخزون المتاح
   - عند الموافقة على الطلب، يتم خصم الكمية من المخزون

---

## 🐛 في حالة وجود مشاكل

### المشكلة: الصفحة لا تعمل
```bash
# امسح الـ cache
php artisan config:clear
php artisan cache:clear
php artisan view:clear
```

### المشكلة: التصميم لا يظهر
```bash
# أعد تشغيل Vite
npm run dev
```

### المشكلة: خطأ في قاعدة البيانات
```bash
# أعد إنشاء قاعدة البيانات
php artisan migrate:fresh --seed
```

---

## 🎉 استمتع بالتطبيق!

التطبيق جاهز للاستخدام والتجربة. جميع المميزات تعمل بشكل كامل!
